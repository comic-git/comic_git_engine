<!-- ai-agent-toolkit:managed version="1.0.0" -->
<!-- Copy this file to docs/integration/<dependency-name>.md when adding a new integration.
     Fill in all sections. Remove sections that don't apply.
     Delete this comment block when done. -->

# <Dependency Name>

**Version / API version:** <!-- e.g., REST API v3, SDK 2.4.x -->
**Used for:** <!-- One sentence: what does this repo use this dependency for? -->
**Canonical docs:** <!-- Link if publicly accessible; note "requires auth" or "not publicly available" if not -->

## Authentication

<!-- How does this repo authenticate with this dependency?
     - Where do credentials live? (env var names, secrets manager path)
     - Token format, rotation policy, any gotchas with auth expiry
     - Do not include actual credentials here — reference where to find them -->

## How We Use It

<!-- Which specific endpoints, operations, or SDK methods does this repo use?
     Focus on what's actually called, not the full API surface.
     Include request/response shapes only if non-obvious or poorly documented. -->

## Environment Differences

<!-- How does this dependency behave differently across environments?
     - Test/sandbox vs. production behavior
     - Any env-specific configuration or API keys
     - Feature flags or capabilities that differ by environment -->

## Local Development

<!-- How to work with this dependency locally:
     - Mocking / stubbing strategy
     - Whether a real test account is needed and how to get one
     - Any local proxy or recording setup -->

## Gotchas

<!-- Non-obvious behavior, known bugs, or things that silently go wrong.
     This is the most important section — capture what the docs don't tell you. -->

## Related Decisions

<!-- Links to any docs/decisions/ files that affect how this dependency is used -->
